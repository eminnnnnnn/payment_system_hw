//change_image_style(elem) {
  // elem.setAttribute("style","box-shadow: none;")
//}

/*move_images_shadow() {
  images = document.getElementsByTag("img");
  for (let i = 0; i < images.length(); i++){
    let image_x = images[i].getBoundingClientRect().left;
    let image_y = images[i].getBoundingClientRect().top;
    if (event.pageX > image_x + 256)
      //if (event.pageY > image_y)
        images[i].setAttribute("style","box_shadow: " + String(event.pageX % image_x) + 'px 5px 5px black;');
  }
}*/
